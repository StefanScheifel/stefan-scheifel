/** @deprecated Import from `@/i18n/types` or use `usePortfolio()` in client components. */
export type {
  PortfolioProject,
  SkillItem,
  LanguageItem,
  TrainingItem,
} from "@/i18n/types";

export { getDictionary, getNavLinks, localePath } from "@/i18n/get-dictionary";
export { defaultLocale, locales, type Locale } from "@/i18n/config";
